<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: Inter, sans-serif;
      margin: 0;
      padding: 0;
    }
    .email-container {
      max-width: 600px;
      margin: 20px auto;
      background-color: #ffffff;
      padding: 20px;
    }
    .header {
      text-align: left;
      padding-bottom: 20px;
      font-size: 22px;
      font-weight: 500;
    }
    .signature {
      display: flex;
      align-items: center;
      margin-top: 20px;
      gap: 10px;
    }
    .signature img {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      float: left;
      vertical-align: middle;
      margin-right: 5px;
      margin-top: 10px;
      object-fit: cover;
    }
    .header img {
      width: 40px;
      height: 40px;
      vertical-align: middle;
      float: left;
    }
    .header p {
      display: inline-block;
      font-size: 18px;
      margin: 0;
      margin-left: 10px;
      vertical-align: middle;
    }
    .content {
      margin: 20px 0;
    }
    .content h2 {
      font-size: 18px;
      color: #333;
      font-weight: 500 !important;
    }
    .content p {
      font-size: 14px;
      line-height: 1.6;
      color: #555;
    }
    a {
      color: #19191C;
    }
    .content a:hover {
      text-decoration: underline;
    }
    .footer {
      font-size: 12px;
      color: #777;
      margin-top: 20px;
      border-top: 1px solid #ddd;
      padding-top: 10px;
    }
    .footer a {
      text-decoration: underline;
      color: #777;
    }
    .footer a:hover {
      text-decoration: underline;
    }
    .social-icons {
      margin-right: 5px;
    }
    .social-icons img {
      width: 30px;
      height: 30px;
      margin: 0 5px;
      vertical-align: middle;
    }
  </style>
</head>
<body>
  <div class="email-container">
    <!-- Header -->
    <div class="header">
      <img src="<%= newsletterIcon %>" alt="Logo">
      <p><%= newsletter %></p>
    </div>
    <!-- Content -->
    <div class="content">
      <h2>Thanks for subscribing to <%= newsletter %></h2>
      <p>Hi,</p>
      <p>
        Thank you for subscribing to <strong><%= newsletter %></strong>! We're thrilled to have you on board. To confirm your subscription please click the buttom below:
      </p>
      <a href="<%= redirectUrl %>" style="border-radius: 5px;border: 1px solid #D8D8DB;background: white;color: #97979B;padding: 5px;">Confirm Subscription</a>
      <div class="signature">
        <img src="<%= author.avatar %>" alt="Logo">
        <p><strong><%= author.name %></strong><br><%= author.position %></p>
      </div>
    </div>
    <!-- Footer -->
    <div class="footer">
      <div class="social-icons">
        <% socials.forEach(function(social){ %>
          <a href="<%= social.url %>"><img src="<%= social.emailImage %>" alt="<%= social.name %>"></a>
        <% }); %>
      </div>
      <p>
        <a href="#">Terms</a> | <a href="#">Privacy</a> | <a href="#">Unsubscribe</a>
      </p>
      <p>
        Deployed by <a href="https://appwrite.io">Appwrite</a>. Source code available on <a href="https://github.com/appwrite/template-for-newsletter">GitHub</a>.
      </p>
    </div>
  </div>
</body>
</html>
